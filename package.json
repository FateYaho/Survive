{
  "name": "game-dev",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "영토 개척 생존기 (가제) — 탑다운 서바이벌 + 타워디펜스 + 로그라이크",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
